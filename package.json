{
  "name": "@amotarao/next-routemap",
  "version": "0.0.1",
  "description": "",
  "bin": {
    "next-routemap": "bin/cli.mjs"
  },
  "files": [
    "bin",
    "dist",
    "src"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "tsup --watch && tsc --watch",
    "build": "tsup && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier . --write"
  },
  "author": "Amon Sawamura <amon.sawamura@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.20",
    "prettier": "3.2.5",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3"
  }
}
